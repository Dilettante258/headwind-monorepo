{
  "name": "@headwind/test-fixtures",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:update": "vitest run --update",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@headwind/transformer": "workspace:*"
  },
  "devDependencies": {
    "@headwind/config": "workspace:*",
    "tsup": "^8.3.5",
    "vitest": "^3.0.4"
  }
}
